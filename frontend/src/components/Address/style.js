import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles({
  outerCard: {
    width: '100%',
    minWidth: '18rem',
  },
  streetAddress: {
    fontWeight: '600',
  },
})
